general:
  branches:
    ignore:
      - gh-pages

machine:
  python:
    version: 2.7

dependencies:
  override:
    - pip install -U .
    - pyenv local 2.7 3.4.3

test:
  override:
    - tox
    - tox -e codacy
  post:
    - mkdir $CIRCLE_ARTIFACTS
    - cp coverage.xml $CIRCLE_ARTIFACTS
